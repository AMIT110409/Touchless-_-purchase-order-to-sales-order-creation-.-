# requirements-gpu.txt
# ============================================================
# GPU-specific dependencies (CUDA 12.1)
# Install with: pip install -r requirements-gpu.txt
# Only needed for local GPU inference (Qwen/Llama models)
# Production uses Claude API (ANTHROPIC_API_KEY) — no GPU needed
# ============================================================

# PyTorch with CUDA 12.1
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchaudio==2.5.1+cu121
torchvision==0.20.1+cu121

# GPU-accelerated model libraries
transformers==4.57.3
accelerate==1.12.0
bitsandbytes==0.49.1
sentence-transformers==5.2.0
safetensors==0.7.0
tokenizers==0.22.2
